#!/bin/bash
# Pandoc is required to convert Markdown files to HTML for publishing.
sudo apt install pandoc
# inotify-tools used to watch the 'text' directory for changes and continuously convert to HTML. Optional.
sudo apt install inotify-tools
